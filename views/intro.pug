extends pagelayout


block content
  .jumbotron.jumbotron-fluid
    .container#jimbo
      blockquote.blockquote.lead#quote
  for section in text
    if (section.textLoc == "left")
      .container-fluid(id = section.id)
        .col-md-5.m-x-0
          for slide in section.slides
            .slide(id = slide.id)
              .slidebox
                h4.slideTitle.text-xs-center #{slide.title}
                p.slideText.text-justify(id = slide.id) #{slide.substance}
        .col-md-7.introAni.m-x-0.hidden-sm-down
    else if (section.textLoc == "right")
      .container-fluid(id = section.id).row
        .col-md-7.m-x-0.introAni.hidden-sm-down
        .col-md-5.m-x-0.push-md-7
          for slide in section.slides
            .slide(id = slide.id)
              .slidebox
                h4.slideTitle.text-xs-center #{slide.title}
                p.slideText.text-justify(id = slide.id) #{slide.substance}
block extrafooter
  link(rel="stylesheet", type="text/css", href="../css/page.css")
  link(rel="stylesheet", type="text/css", href="../css/intro.css")
  script(src="https://d3js.org/d3.v4.min.js")
  script(src="https://d3js.org/d3-scale-chromatic.v1.min.js")
  script(type="text/javascript"  src="https://cdn.mathjax.org/mathjax/latest/MathJax.js?config=TeX-AMS-MML_SVG")
  script(type="text/x-mathjax-config").
    MathJax.Hub.Config({
    tex2jax: {
    inlineMath: [ ['$','$'], ['\\(','\\)'] ]
    }}
    );
  script(src="../javascripts/mods/svg_mathjax.js")
  script(src="../javascripts/introViz.js")
